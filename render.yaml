databases:
  - name: ewt-db
    databaseName: ewt_db
    user: ewt_user
    plan: free

services:
  # Backend Web Service (Django)
  - type: web
    name: ewt-backend
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn ewt_project.wsgi:application"
    rootDir: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ewt-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://ewt-frontend.onrender.com"
      - key: USE_POSTGRES
        value: "True"
      - key: PYTHON_VERSION
        value: "3.11.6"
      - key: DEV_LOGIN_ENABLED
        value: "True"

  # Frontend Web Service (Next.js)
  - type: web
    name: ewt-frontend
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    rootDir: frontend
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: "https://ewt-backend-t98q.onrender.com"
      - key: NODE_VERSION
        value: "20.11.0"
