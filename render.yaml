databases:
  - name: ewt-db
    databaseName: ewt_db
    user: ewt_user
    plan: free

services:
  # Backend Web Service (Django)
  - type: web
    name: ewt-backend
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn ewt_project.wsgi:application"
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ewt-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        fromService:
          type: web
          name: ewt-backend
          property: host
      - key: CORS_ALLOWED_ORIGINS
        fromService:
          type: web
          name: ewt-frontend
          property: url
      - key: USE_POSTGRES
        value: "True"

  # Frontend Static Site / Web Service (Next.js)
  - type: web
    name: ewt-frontend
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    rootDir: frontend
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: ewt-backend
          property: url
